apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecivres.fullname" . }}
  labels:
    {{- include "ecivres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecivres.selectorLabels" . | nindent 4 }}
